apply plugin: "java"
apply plugin: "com.zyxist.chainsaw"

dependencies {
	testCompile 'junit:junit:4.12'
}

repositories {
	jcenter()
}

afterEvaluate {
	javaModule.extraTestModules = ['org.mockito']
}